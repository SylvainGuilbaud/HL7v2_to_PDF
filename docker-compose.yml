services:
  iris:
    build: 
      context: .
    restart: always
    container_name: hl7-to-pdf-iris
    hostname: hl7-to-pdf-iris
    ports:
      - 1972
      - 52773
      - 53773
    command: --check-caps false
    volumes:
      - ./data:/data



